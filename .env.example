# Optional metadata for production deployments.
# SEC fetches already use the required dashboard User-Agent in lib/constants.ts.
NEXT_PUBLIC_APP_URL=http://localhost:3000

# If you later replace the market price fetcher with a paid provider, document the key here.
# MARKET_DATA_API_KEY=
